NVC HOME4YOU — IMPROVEMENTS & IDEAS TRACKER
============================================
(kept up to date as we work; [ ] = todo, [x] = done, [~] = in progress)

Current state and what to do next -> HANDOFF-2026-08-17.md
Release mechanics -> DEPLOY.md   (note the prerender step, §6b)
Migration history -> ROADMAP-datalayer-admin.md

RECENTLY DONE
-------------
[x] Saved configurator links on Azure SQL (2026-08-15)
    The last table off Quickbase. Migration applied; import DONE 2026-08-17 (19 rows).
    Only DATA_SOURCE_SAVEDCONFIGS=sql in App Service is left.
    SQL misses fall back to Quickbase, because the codes are already in customers' inboxes.
    Once DATA_SOURCE_SAVEDCONFIGS=sql is live and the import has run, Quickbase has no
    live runtime path left.
[x] Prices page — /bg/ceni, /en/prices, /el/times (2026-08-15)
    The whole catalogue: 8 box houses + 6 wagons, cards and a table per section, priced
    FROM the gallery so the page and the product pages can never disagree. Targets
    "сглобяеми къщи цени", the highest-intent query in the market.
    Box assembly is ADDED; a wagon's price already CONTAINS its EUR 1000 assembly.
[x] Configurator strip on the home page (2026-08-15)
    Three model cards with real photos and finished prices; tapping one opens the
    configurator with that model preselected (?model=37|58|73).
[x] SEO audit and fixes (2026-08-14/15) — see HANDOFF-2026-08-17.md
    - 42 product pages were canonicalising to the HOMEPAGE. Fixed at request time.
    - Body prerendering: 55/55 routes, ~140k chars of crawlable text where every URL
      previously served 0. Competitors served 4,819-19,392.
    - FAQ structured data declared 6 answers that were not in the DOM at all. Fixed.
    - Bare domain was a fourth homepage; sitemap now generated from paths.js (it had
      silently never listed the services page); HSTS; a Greek page canonicalised onto a 404.
    - Still to do by hand: Search Console resubmit + re-index request.
[x] Customers, factories and purchases in the admin panel (2026-08-14)
    Customer identity (физическо/юридическо, ЕИК/ЕГН), purchases as a join to factories,
    deposit/final price/left-to-pay, prepaid + final invoice attachments.
    Migration applied to production 2026-08-14.
[x] Leads pipeline (2026-08-12/13) — see HANDOFF-2026-08-12.md
    Inquiry queue + lead pipeline with conversation threads, attachments both ways,
    follow-up dates and an emailed due report, AI-drafted replies. 257 CRM leads imported.
[x] Gallery + cases on Azure SQL, all images on Azure Blob as WebP (2026-08-06)
    ~92 MB saved converting images to WebP. Tag deploy-2026-08-06.
[x] Reviews migrated to Azure SQL + admin panel (2026-08-05)
[x] Mobile-first redesign of the Box House configurator
[x] Homepage testimonials / social proof near CTAs

HIGH PRIORITY — MEASURE & CONVERT
---------------------------------
[x] 1. Funnel analytics events (PR #1) — GTM-MSJR4ZGP live
[x] 2. Save & resume / shareable configuration (PRs #2/#3)
[ ] 3. Popular presets / quick-start bundles in the configurator
       (3-4 ready-made configurations to pick and tweak; fights choice overload)
       Partly addressed by the home-page configurator strip, which now makes the FIRST
       choice for the visitor. Presets would take it further.
[x] 4. Speed-to-lead follow-up (PR #8)
[ ] 5. Financing / total-cost calculator
       (monthly payment estimate; clear "included vs quote-on-review" breakdown)
       Cheaper now: the prices page already computes finished totals per model.

COMPOUNDING BETS
----------------
[~] 6. Content/SEO engine: blog + guides + location pages
       Technical SEO is done (see above). What is left is genuinely content:
       - product pages are ~135 words, /en/modular-houses is 278
       - the two comparison articles: "сглобяема къща или тухла",
         "колко струва сглобяема къща" — low difficulty, high intent, and both
         feed the prices page internally
[ ] 7. Trust & proof: case studies with real timelines/photos, "homes delivered" counter,
       warranty/certifications page, embedded Google reviews
[ ] 8. PWA: add service worker (vite-plugin-pwa) -> installable, fast repeat visits

FOUNDATION / POLISH
-------------------
[~]  9. Performance pass: Lighthouse / Core Web Vitals, image optimization, preconnect
        Images are WebP, capped at 2560px, served from our origin with a year of immutable
        caching. Prerendering now also means the first paint has real content.
        Still open: per-breakpoint srcset - a phone downloads the desktop-sized file.
[ ] 10. Accessibility audit (EU Accessibility Act applies since June 2025)
[ ] 11. Translation completeness audit for Greek
[x] 12. Dev hygiene: stop tracking api-dotnet bin/ and obj/ build artifacts in git
[ ] 13. Upgrade path for @vitejs/plugin-react (v6 supports vite 8) — only when needed

ADMIN PANEL — WHAT IS STILL MISSING
-----------------------------------
The panel now covers: home, inquiries, leads, customers, factories, reviews, gallery, cases.

[ ] 14. Audit log — who changed what, when.
        Flagged when the concern was "you edit live pricing"; sharper now that the panel
        holds ЕГН, ЕИК, deposits and invoices with no record of who touched them.
[ ] 15. Factory sheet into the admin panel.
        Still at /internal/factory-sheet, localStorage-only: the data exists in exactly one
        browser and a cleared cache loses it. Doubly confusing now that a real Factories
        section sits beside it in the nav.
[ ] 16. Documents section (brochures via Blob) — changing a PDF still needs a developer
        and a deploy. Shares everything with the image pipeline already built.

INFRASTRUCTURE
--------------
[~] 20. www.nvc-home4you.eu fails its TLS handshake — www is not bound as a custom domain
        in App Service. DNS is already correct. Redirect code DONE (CanonicalHost.cs,
        18 tests); left: one GoDaddy TXT record, add the custom domain, add a managed cert.
        Exact values and the ordering trap -> HANDOFF-2026-08-17.md, "www — diagnosed".
        Worse than it sounds: HSTS includeSubDomains makes it unbypassable for anyone who
        has visited the apex in the past year.
[ ] 17. App SQL login is still `dbadmin`, the server administrator — far more privilege
        than it needs. Carried since the start of the migration.
[ ] 18. Managed identity for Blob instead of a stored account key.
[ ] 19. Secret expiry ~2027-02-04: Entra client secret, Graph credentials, Quickbase token.
        Each fails silently and partially. The Quickbase one stops mattering once saved
        configs are live on SQL.

DELIBERATELY NOT DOING
----------------------
- Homepage H1 stays the slogan "Защото домът не трябва да е лукс" even though it carries
  no target keyword. It is brand copy in three languages; that is a business call.
- Billing/invoicing stays in Quickbase — out of scope for the data migration.
